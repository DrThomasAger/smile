# .readthedocs.yaml  — MkDocs build from repo root
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      packages:
        - mkdocs
        # uncomment if you use a theme or plugins
        # - mkdocs-material
        # - mkdocs-awesome-pages-plugin

# Disable PDF/ePub; enable later if you need them
formats: []
